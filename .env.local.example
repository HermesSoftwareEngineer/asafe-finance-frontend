# ─────────────────────────────────────────────────────────────────────────────
# Asafe Finance — Frontend — Variáveis de Ambiente
# Copie este arquivo para .env.local (já ignorado pelo git).
#
#   cp .env.local.example .env.local
#
# ─────────────────────────────────────────────────────────────────────────────

# URL do backend FastAPI
# Dev local: proxy do Vite cobre /api → não precisa setar
# Prod (Vercel): aponte para a URL do Cloud Run
VITE_API_URL=https://seu-backend.run.app
